import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { motion } from "framer-motion";

const Home = () => {
  return (
    <div className="p-6 max-w-6xl mx-auto">
      <motion.h1 
        className="text-4xl font-bold text-center mb-6" 
        initial={{ opacity: 0 }} 
        animate={{ opacity: 1 }} 
        transition={{ duration: 1 }}
      >
        Luxury Caravanning in Wales
      </motion.h1>
      
      <motion.p 
        className="text-lg text-center mb-8" 
        initial={{ opacity: 0 }} 
        animate={{ opacity: 1 }} 
        transition={{ duration: 1.5 }}
      >
        Discover the most stunning caravan parks and budget-friendly luxury stays.
      </motion.p>
      
      <div className="grid md:grid-cols-3 gap-6">
        {[1, 2, 3].map((item) => (
          <Card key={item} className="shadow-lg rounded-2xl overflow-hidden">
            <img 
              src={`https://source.unsplash.com/400x300/?caravan,travel${item}`} 
              alt="Luxury Caravan" 
              className="w-full h-48 object-cover"
            />
            <CardContent className="p-4">
              <h2 className="text-xl font-semibold mb-2">Luxury Caravan Park {item}</h2>
              <p className="text-sm text-gray-600 mb-4">A perfect getaway in Wales with premium amenities.</p>
              <Button className="w-full">Explore</Button>
            </CardContent>
          </Card>
        ))}
      </div>
      
      <div className="mt-10 text-center">
        <h3 className="text-2xl font-semibold">Get Exclusive Deals</h3>
        <p className="text-sm text-gray-500">Sign up for the latest caravan holiday discounts!</p>
        <input 
          type="email" 
          placeholder="Enter your email" 
          className="mt-4 p-2 border rounded-lg w-80 text-center"
        />
        <Button className="ml-2">Subscribe</Button>
      </div>
    </div>
  );
};

export default Home;
